export default function memoize(func) {
    let result;
    let hasResult = false;
    return () => {
        if (!hasResult) {
            result = func();
            hasResult = true;
        }
        return result;
    };
}
//# sourceMappingURL=memoize.js.map